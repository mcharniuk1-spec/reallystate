Pattern:
Property-link investigation must remain operator-reviewed before any canonical merge or public claim.
Evidence:
The 2026-05-29 comparable-search layer converts one URL into a source-publication fingerprint, blocks grouped/development pages from single-property promotion, defaults to accepted-only saved tier 1/2/3 evidence, excludes same-source rows by default, and emits scored evidence/conflicts as a JSON report instead of merging records.
Implication:
Future agents should treat link-based comparable search as the bridge between scraping and entity-resolution review, not as an automatic dedupe or publication system.

Pattern:
Strategic planning knowledge for Real Estate Bulgaria should be a separate vault entity.
Evidence:
The project now has many active agent lanes, branch lines, source gates, and operator commands; putting durable sequencing logic only in chat or TASKS creates repeated context loss.
Implication:
Future planning changes should update the vault Strategic Planning Knowledge Entity when they alter sequence, ownership, gates, or cross-agent dependencies.
