# Claude Skill Mirror

Canonical project skill instructions live in `agent-skills/<skill-name>/SKILL.md`.

When using Claude Code, either copy the needed skill folder into this directory or tell Claude to read the canonical `agent-skills` path directly. The canonical files are intentionally plain Markdown so Cursor, Codex, and Claude can share them without duplication drift.
